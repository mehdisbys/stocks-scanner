# Secrets — never commit
.env
config.yaml
# Google service-account key(s) for `scan_base_div --gsheet`
*.json
scanner-*.json

# transient helper scripts
_*.py

# Local data cache (Parquet + SQLite)
data/

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# transient image encodings
*.b64
